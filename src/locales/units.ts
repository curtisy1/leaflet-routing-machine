import type { Units } from './types';

const defaultUnits: Units = {
  meters: 'm',
  kilometers: 'km',
  yards: 'yd',
  miles: 'mi',
  hours: 'h',
  minutes: 'min',
  seconds: 's',
};

export default defaultUnits;
